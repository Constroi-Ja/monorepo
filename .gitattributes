# Default: normalize all text files to LF on checkout
* text=auto eol=lf

# Shell scripts must always use LF
*.sh text eol=lf

# Windows-specific files can use CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
